<Window x:Class="WCFChatClient.EnterNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WCFChatClient"
        mc:Ignorable="d"
        Title="EnterNameWindow" Height="450" Width="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock VerticalAlignment="Center">Choose Name: </TextBlock>
        <TextBox Name="txtName" Grid.Column="1" MinWidth="100" Margin="10" Padding="5" ></TextBox>
        <Button Grid.Row="1" Grid.ColumnSpan="2" Padding="10" Name="cmdJoin" Click="CmdJoin_Click">Join</Button>
    </Grid>
</Window>
